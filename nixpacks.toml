[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.build]
cmds = ["echo 'Build phase completed - using pre-built files'"]

[start]
cmd = "node server.js"

[variables]
NODE_ENV = "production"
PORT = "3001"