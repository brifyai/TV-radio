# 🔑 CONFIGURACIÓN CORREGIDA TOKEN CLOUDFLARE

## 📋 CONFIGURACIÓN REAL (OPCIONES DISPONIBLES):

### 1. **NOMBRE DEL TOKEN:**
```
TV-radio HTTPS Setup
```

### 2. **PERMISOS DISPONIBLES:**

**Account:**
- Ninguno necesario

**Zone:**
- `Zone:Zone:Read` ✅
- `Zone:Zone:Edit` ✅

**DNS:**
- `DNS:DNS:Read` ✅
- `DNS:DNS:Edit` ✅

### 3. **RECURSOS:**
En la sección "Zone":
- Seleccionar `Include - All zones` ✅
- O seleccionar específicamente `147.93.182.94.sslip.io`

### 4. **FILTRO DE DIRECCIONES IP:**
**Dejar vacío** (sin restricciones de IP)

### 5. **TTL:**
- **Start Date:** Hoy (fecha actual)
- **End Date:** 1 año desde hoy

## 🎯 CONFIGURACIÓN CORREGIDA:

```
Nombre del token: TV-radio HTTPS Setup

Permisos:
✅ Zone:Zone:Read
✅ Zone:Zone:Edit
✅ DNS:DNS:Read
✅ DNS:DNS:Edit

Recursos:
✅ Include - All zones
(o seleccionar específicamente: 147.93.182.94.sslip.io)

Filtro IP: (vacío)

TTL:
Start: Hoy
End: 1 año
```

## ⚠️ NOTA IMPORTANTE:

**Los permisos SSL/TLS se configuran automáticamente** cuando seleccionas:
- `Zone:Zone:Read` y `Zone:Zone:Edit`

Esto le da acceso completo a todas las configuraciones de la zona, incluyendo SSL/TLS.

## 🔄 PASOS PARA COMPLETAR:

1. **Nombre:** `TV-radio HTTPS Setup`
2. **Permisos:** Seleccionar solo los 4 permisos de arriba
3. **Recursos:** "Include - All zones"
4. **IP Filter:** Dejar vacío
5. **TTL:** Fechas como se indica
6. **Crear Token**
7. **Copiar el token generado**

## 🚀 DESPUÉS DE CREAR EL TOKEN:

Compárteme el token y yo ejecuto:
```bash
export CLOUDFLARE_TOKEN="tu_token_aqui"
node cloudflare-quick-setup.js
```

## ✅ ESTO CONFIGURARÁ AUTOMÁTICAMENTE:

- DNS: CNAME proxied
- SSL: Full (strict)
- Always Use HTTPS: ON
- HSTS: Configurado

¡Listo! Con estos permisos es suficiente para toda la configuración HTTPS.